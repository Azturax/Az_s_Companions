# Dev quarantine

Files here are **not** compiled into the mod jars.

| File | Notes |
|------|--------|
| `CompanionRecentActionHooks.java.wip` | Draft shared hooks; live wiring is in NeoForge `CompanionRecentActionEvents` / Fabric `FabricCompanionRecentActionEvents` (+ mixins). |
